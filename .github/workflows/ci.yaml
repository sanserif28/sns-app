name: CI

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE: ghcr.io/sanserif28/sns-app

jobs:
  build:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - uses: gradle/actions/setup-gradle@v4

      - name: Build & Test
        run: ./gradlew build

      - id: meta
        name: Resolve image tag
        run: echo "version=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Build & Push image
        run: |
          ./gradlew jib \
            -Djib.to.image=${{ env.IMAGE }}:${{ steps.meta.outputs.version }} \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

  update-manifest:
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          repository: sanserif28/sns-devops
          token: ${{ secrets.DEVOPS_TOKEN }}

      - name: Update image tag
        run: |
          IMAGE_TAG="${{ needs.build.outputs.image-tag }}"
          sed -i "s|image: .*sns-app.*|image: ${{ env.IMAGE }}:${IMAGE_TAG}|" k8s/sns-app/app.yaml

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/sns-app/app.yaml
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "deploy: sns-app ${GITHUB_SHA::7}"
          git push
